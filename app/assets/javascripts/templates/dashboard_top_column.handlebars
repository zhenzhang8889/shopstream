{{#with view}}
  <header class="text-center">
    <h4>Top {{title}}</h4>
    <h4 class="subheader short">{{subtitle}}</h4>
  </header>
  <article>
    {{collection SS.DashboardTopCollectionView contentBinding="topItems"}}
  </article>
{{/with}}
